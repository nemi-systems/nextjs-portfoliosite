# Ontology Viewer

Static Next.js viewer for `ontology-builder` export artifacts.

## Refresh Data

```bash
npm run import:artifact
```

Use `--source <path>` to import a different `publish/current.json`.

## Build

```bash
npm run build
```
